# Column-Transformer
The Column Transformer is a powerful tool in scikit-learn that allows you to apply different preprocessing steps to specific columns within a dataset. It helps combine transformations like scaling numerical features and encoding categorical variables into a single pipeline, ensuring all features are processed consistently before model training.
